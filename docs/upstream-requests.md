# Upstream requests — the record of what porting asked the framework for

_This file was `pr/README.md` until 2026-08-18. The directory held nothing
else by then, and a folder called `pr/` at a repository root reads as "pull
requests" to everyone who did not write it. `CAPABILITIES.md` cites the rows
below by their request id — `pr/<id>` — and those ids are unchanged._

**The open requests have moved.** They live in
[`abap2UI5/abap2UI5` under `backlog/`](https://github.com/abap2UI5/abap2UI5/tree/main/backlog)
now — four backlogs, sorted by where each one gets filed:

| | |
|---|---|
| [OPEN-ABAP.md](https://github.com/abap2UI5/abap2UI5/blob/main/backlog/OPEN-ABAP.md) | `open-abap/open-abap-core`, `abaplint/transpiler` |
| [ABAPLINT.md](https://github.com/abap2UI5/abap2UI5/blob/main/backlog/ABAPLINT.md) | `abaplint/abaplint` |
| [ABAP2UI5-LINTER.md](https://github.com/abap2UI5/abap2UI5/blob/main/backlog/ABAP2UI5-LINTER.md) | `abap2UI5/linter` |
| [ABAP2UI5.md](https://github.com/abap2UI5/abap2UI5/blob/main/backlog/ABAP2UI5.md) | the framework |

A request about the framework sat in the corpus, findable only by somebody who
already knew the corpus existed — and the same folder held requests for three
*different* upstreams with nothing distinguishing them. The five that were open
on 2026-08-17 were carried over verbatim: `open-abap-xml-escaping`,
`transpiler-returning-is-supplied`, `linter-sapui5-metadata`,
`event-auto-model-update`, `frontend-action-named-api`.

**The convention is unchanged, only its destination is** (AGENTS.md §10 /
TRAINING.md "Distill"): whenever porting or reviewing surfaces an improvement
idea for the framework — a missing API parameter, a capability marked ❌ in
CAPABILITIES.md that upstream could close, a behavior gap — write it up **in
the same change**, as `backlog/items/<id>.md` in `abap2UI5/abap2UI5`. An item
without a case that actually happened is a wish; the gate there requires the
evidence.

**What stays here** is the two tables below: what was asked for and shipped,
and what was asked for and declined. They are this repository's own record —
every row came out of a port in `meta/`, and the "Implemented" table is what
`CAPABILITIES.md` cites when it says a capability exists because somebody asked
for it. Nothing below is open work.

## Declined / deferred (folders removed)

| Request | Decision |
|---------|----------|
| urlhelper-abap-api | **Withdrawn 2026-07-22 — the premise was wrong.** The `URLHELPER` frontend action *is* callable from ABAP: `cs_event-urlhelper` with `TRIGGER_TEL`/`TRIGGER_SMS` (number as a plain string param) and `TRIGGER_EMAIL`/`REDIRECT` (a `{ EMAIL/URL, … }` object-literal `t_arg` — `get_t_arg` emits `{`-prefixed args raw as UI5 event-handler object literals). App 084 ported 1:1 this way; apps 041/073's external links switched from the (same-origin-only) `open_new_tab` to `urlhelper` REDIRECT. Captured as a CAPABILITIES.md convention instead of a framework change |
| named-json-models | **Too complicated with the current abap2UI5 approach** — every view slot serializes exactly one ABAP-fed default JSONModel per roundtrip; a second named model would have to be carried through bind, serialization and model-update on every slot. Declined for now, possibly worth re-discussing in the future if the model layer changes. Workaround stays: flatten into the default model or resolve statically (IMPROVISED deviation, apps 006/031/046, same family app 044) |
| app-lifecycle-base-class | **Declined 2026-08-11 — deliberate: abap2UI5 apps stay interface-only.** The proposal was an optional abstract `z2ui5_cl_app` with `on_init`/`on_event`/`on_navigated` hooks, to remove the identical 12-line dispatcher every app hand-writes (366/366 classes here, ~4.4k lines in this corpus). Maintainer decision: **too much overhead for the gain** — an app implements `z2ui5_if_app` and nothing else, and the dispatcher boilerplate is accepted in exchange for keeping the app contract a single interface with no inheritance, no base-class lifecycle to learn and no second way to write an app. Do not re-propose a base class, and do not add lifecycle hooks to `z2ui5_if_app`; the dispatcher stays hand-written per app (pattern-lint keeps its shape uniform here) |
| message-manager-binding | **Half already covered, half implemented** — first recorded (b07) as "not filed, already covered": reading messages IS covered by the `message>` auto-collection (2026-07-18) and the plain-table MessagePopover (app 038). But porting `MessagePopoverMessageHandling` (b08) showed the **write** half — the controller's `MessageManager.addMessages` with custom text/target — was not expressible. Implemented 2026-07-20 as the `z2ui5.cc.MessageManager` companion control (see Implemented table) |

## Implemented (folders removed)

| Request | Implemented | Upstream result |
|---------|-------------|-----------------|
| linter-view-builder-attribute-verb | 2026-08-14 | `lib/builders.mjs` spells the view builder's attribute verb `a( )`, the name abap2UI5#2580 (`6c5c5aa`) renamed `att( )` to. Before the fix the reconstructor matched no attribute call at all on a migrated port, so the `xmlns` declarations went with the rest and all 416 ports failed with "namespace prefix is used but never declared" — a corpus-wide red that had nothing to do with any port. Landed together with the removal of the superseded builder from `lib/builders.mjs`, which is what made the one-letter verb unambiguous |
| control-method-named-removeall | 2026-08-10 | merged upstream as abap2UI5 **#2535**: the denylist is split in two, because its halves need different matching. A GENERIC reflection mutator takes the member NAME as an argument and therefore has exactly ONE spelling, so it is denied by exact name (`CONTROL_METHOD_DENY_EXACT`) - matching it by prefix could only swallow the named methods that share those characters, which is what `removeAll` did to `removeAllSelectedDates`/`removeAllItems`/`removeAllContent` and `destroy` to `destroySpecialDates`. Everything hostile in every spelling (`bindProperty` as much as `bind`) stays a prefix. The Association twins the prefix form had left uncovered (`addAssociation`, `removeAssociation`, `removeAllAssociation`) and `destroyAggregation` are now denied explicitly. App **307** clears its Calendar 1:1 again - `sap.ui.unified.Calendar.selectedDates` is written by the control itself, so the port had no bindable alternative at all - and its `IMPROVISED` deviation is gone. The linter mirrored the split in the same round (`lib/frontend-actions.mjs`, linter `79c6fbf`) |
| linter-openui5-1151 | 2026-08-10 | merged upstream as linter **#15** (`5695839`): all `@openui5/*` pins moved 1.150.0 -> 1.151.0 and `data/properties.json` was regenerated with the same generator (970 -> 988 controls, `ui5Version` 1.151.0). The point is that a stale pin does not under-report, it MIS-reports: a member released after the pin is not `member-too-new` (a version verdict a `POST_171` deviation can knowingly excuse) but `unknown-property` — "typo?" — plus a render load failure, and nothing can excuse those, because that is the shape a real typo has. The 1.71 floor the gate checks against is a separate parameter (`minUi5`), so the bump changes no verdict on existing code — measured, not assumed: view-gates over this corpus is byte-identical before and after. It unblocks `sap.tnt.sample.SideNavigationSearch`, whose three members (`SideNavigationSearchField`, `SideNavigation.filterSection`, `NavigationList.highlightedText`) are all @since 1.151 |
| card-manifest-object | 2026-08-10 | `z2ui5_if_client~_bind( json = abap_true )` - a bound string that already CONTAINS JSON is spliced into the model as a JSON **node** instead of being sent as a quoted string. It exists for a control property that must receive an **object**, which no typed ABAP value can be: `sap.ui.integration.widgets.Card.createManifest` branches on the JS type and reads a STRING manifest as a manifest **URL**, while a manifest's keys (`sap.app`, `sap.card`, `_version`) are not valid ABAP field names, so it is no structure either - an inline manifest had no route to the control at all. Outbound only (such a payload is rendered, never authored, by the client, and reading the node back would mean writing an object into a string field), and an unparseable string raises instead of emitting broken JSON. Apps **118** and **341** bind their seven / eleven inline card manifests through it; app 118's eighth card and apps 342/350, whose manifests are one file each, keep the plain URL bind that always worked |
| aggregation-item-address | 2026-08-06 | wherever a `CONTROL_BY_ID` argument takes a control ID it now also takes an aggregation ITEM, addressed positionally as `<id>/<aggregation>/<index>` (`carousel/pages/2`, 0-based) - the equivalent of the UI5 controller idiom `oCarousel.setActivePage( oCarousel.getPages()[ i ] )`, which no id-based call can express. **The premise was half wrong and the probe said so** (`scripts/probes/aggregation-item-probe.mjs`): clone ids are NOT nondeterministic, as three sidecars claimed - UI5 mints `<templateId>-<parentId>-<index>` and they survive a model refresh AND a reorder. The gap is that the parent id carries the view prefix assigned at runtime (`v1--tpl-v1--car-0`), which the backend never sees. Resolution therefore happens on the client, where the prefix and the aggregation are both known, and it serves every control-resolving argument kind (`controlId`, `controlIdOrNull`, `anchor`, `within`) at once. An out-of-range index or a single-valued aggregation is reported instead of silently passing `undefined`. App **012** re-syncs its two Carousels again |
| keyboard-shortcut-scope | 2026-08-06 | `cs_event-keyboard_shortcut` takes an optional THIRD `t_arg`: a view slot to scope the registration to. The registry was keyed by combination alone, so a second registration of the same combination replaced the first and there was no way to say "only while the popover is up" - which is exactly what the sample's `CommandExecution` in a Popover's `dependents` does to the page-level one for the same command. Dispatch now picks the innermost OPEN scope (`ViewSlots.getView` is the is-open test), the unscoped form is unchanged, unregistering is per scope, and a combination whose last entry is gone is dropped so the listener stops swallowing the browser's own Ctrl+S. App **232** registers `Ctrl+S` twice - unscoped -> `SAVE`, popover-scoped -> `PSAVE` - so the popover's own enabled/visible flags gate the command exactly while it is open. Its last residual deviation is closed |
| conditional-prevent-default | 2026-08-05 | `s_ctrl-prevent_default_expr` - the event veto decided per FIRING instead of per wire. `check_prevent_default` is a boolean baked at render time, so it could not block one row/column and let the rest through the same event; the new field carries a client expression resolved by `BindingParser.parseExpression` like every other `$`-prefixed event argument. `eBP`'s second argument became the condition, so the flag form (which now sends the constant `true`) is byte-identical and the round-trip payload is untouched in both. App **247** resizes 1:1 again - the delivery-date column is blocked, every other one reports its LABEL and new width, both of which the earlier reduction had dropped. Only the original's 50ms message BUFFER stays dropped |
| live-device-model | 2026-08-05 | the shared `device>` model refreshes on resize, rotation and breakpoint change. It wrapped the LIVE `sap.ui.Device` object all along, but a JSONModel only notifies its bindings when told - measured `1400 -> 1400` across a real 1400px->420px viewport change, `1400 -> 420` with the refresh. It also publishes **`/media/range`** (`Phone`/`Tablet`/`Desktop`), because `Device.media` exposes methods only and has no bindable property: app **168**'s `attachLayoutChange` class swap is a plain expression binding now, live and round-trip-free. `Device.system` deliberately stays static (UA/screen based, not viewport based), so every existing `{device>/system/phone}` branch is unaffected. App **012** is NOT closed by it and says so: its page count feeds a server-side slice |
| model-empty-vs-default (path-scoped half) | 2026-08-05 | `_bind( omit_initial_paths = VALUE #( ( `MIN` ) ( `MAX` ) … ) )` - the same omission scoped to the listed columns, because the blanket flag cannot express one shape: a BOOLEAN that has to send `abap_false`. False is itself initial, so `omit_initial` dropped it and the control fell back to its default `true`. Implemented as a local ajson filter in the core_client class pool (matching nodes by NAME, the last path segment, which is how a bound row is addressed in the view) and ANDed with a caller filter like the blanket form. App 049 now binds `enabled`/`editable` as plain `{ENABLED}`/`{EDITABLE}` again - its expression-binding workaround is gone and the port has **no binding-value deviation left** |
| model-empty-vs-default (per-bind half) | 2026-08-05 | `z2ui5_if_client~_bind( omit_initial )` - an INITIAL ABAP field stays absent from the serialized model, so the control keeps its UI5 default instead of receiving `` (which an enum-typed property rejects outright). Implemented by wiring ajson's `create_empty_filter( )` into the `custom_filter` slot `main_json_stringify` already evaluates; a caller-supplied filter is kept (AND). App 049 is rebuilt as the sample's ONE bound `CustomListItem` template - it was 14 unrolled static items - and the dropped template property `valueState` is back. The path-scoped half followed the same day - see the row above |
| control-inline-style | 2026-08-05 | `CONTROL_METHODS` gained `css: [property, value]` - not a UI5 method but a frontend capability in method form: it writes ONE whitelisted declaration (width/height/color/background-color/font-size/opacity, …) onto the control's own DOM ref, for a value no property carries. Apps 138/267/269 wire their width Slider to it roundtrip-free (`${$parameters>/value} + '%'`, an arg being a full UI5 expression), so `sap.m.Page`, which has no `width`, resizes like in the original jQuery handler. App 250's icon tint stays dropped: it reaches into a control's INTERNAL DOM (`getDomRef().firstChild.firstChild`), which the entry deliberately does not offer |
| table-set-sticky | 2026-08-05 | `setSticky: ["object"]` in `CONTROL_METHODS` - the method was reachable through the generalized allowlist but `castArgAuto` inferred a string, so `sap.m.ListBase` rejected the array silently. **Correction to the request's own premise:** it claimed neither the bound-array path nor a whitelist entry was proven - but app 009 binds `sticky` to an ABAP string table and is live-verified, so the bound path DID work all along. The entry closes a footgun (a silently-broken imperative call) rather than an unreachable feature, and apps 022/235, which deleted the sticky Label + three CheckBoxes from their view, are plain REWORK against app 009's proven pattern - tracked in STATUS.md, not here |
| control-method-null-arg | 2026-08-05 | a general `controlIdOrNull` argument kind (`setSelectedSection`, `setSelectedItem`): an EMPTY argument arrives as `null` instead of the `false` `castArgAuto` inferred, generalizing the "empty means null" contract that existed for the `within` kind only. A second fix followed the same day: `get_t_arg` DROPS a trailing empty argument, so the call arrived with no argument at all - `castArgs` now pads a missing trailing argument when (and only when) its declared kind is nullable. App 263 clears the association 1:1 instead of naming the first section |
| invisible-message-announce | 2026-08-05 | `INVISIBLE_MESSAGE` in `GLOBAL_TARGETS` (`announce: [text, mode]`), lazy-required because `sap/ui/core/InvisibleMessage` is @since 1.78. It is a singleton with no control id, so this is the only route to an ARIA live announcement for a backend-driven content change - which is what the framework does by definition. App 289 announces its regenerated MessageStrip assertively, with the original's text |
| custom-currency-formatting | 2026-08-05 | `FORMATTING` in `GLOBAL_TARGETS` (`setCustomCurrencies` / `addCustomCurrency`), lazy-required (@since 1.120). A currency's digit count is neither a control property nor something the curated formatter could register for the standard `sap.ui.model.type.Currency`. App 196 registers BGN4/WWWW on init, so list five renders 4 and 5 decimals like the original |
| menu-item-selected-path | 2026-07-31 | **Closed by option 2 — documented capability boundary; no framework change.** Option 1 was tried first and *almost* worked: `EventHandlerResolver` hands the whole handler string to `BindingParser.parseExpression` (`sap/ui/core/mvc/EventHandlerResolver.js`), so a `$`-prefixed arg is a **full UI5 expression** — method calls, `isA('…')`, concat, ternaries — a capability the corpus had never used and now documented in CAPABILITIES. But measuring the live control tree (browser probe + e2e on OpenUI5 1.152) killed the breadcrumb itself: `sap.m.Menu` re-parents items through an internal `sap.m.MenuWrapper`, and the hop count to the parent `MenuItem` changes with runtime state — **two** hops while the submenu is closed, **four** once its popover exists — while an expression has no loop. The same wrapper breaks the sample's own `while (oItem instanceof MenuItem) … getParent()` loop, so upstream now toasts the leaf text as well: apps 060/061 keep `${$parameters>/item}.getText()` (their deviations became NOTEs, since that is behaviour-identical with the live sample) and the 060 e2e interaction asserts the nested-selection leaf toast to guard the boundary |
| popup-within-area | 2026-08-02 | `POPUP` added as a `CONTROL_GLOBAL` target with `setWithinArea` (`app/webapp/core/FrontendAction.js`), plus a `within` argument kind: a control id resolves to the CONTROL (which `Popup.convertWithin` dereferences when a popup opens, so the area survives a re-render), an EMPTY argument passes `null` and releases the restriction. The module is resolved lazily like `THEMING` because `setWithinArea` is @since 1.89, so an older runtime hits the existing "not available" guard instead of failing the component load. App 285 wires it; the linter's `GLOBAL_TARGETS` copy was updated in the same round. Ordering learned here: a follow-up action runs AFTER the popup of the same round-trip has opened, so the area is set together with the view |
| control-call-whitelist | 2026-07-18 | `open`, `close`, `setExpanded` added to the `CONTROL_BY_ID` whitelist (`app/webapp/core/FrontendAction.js` `CONTROL_METHODS`); ports 469/471 (pre-renumber ids) converted, IMPROVISED deviations dropped. 2026-07-19: the interim `control_call`/`control_call_by_id` wrapper methods were consolidated into `follow_up_action` + `cs_event-control_global`/`control_by_id` |
| message-box-dependent-on | 2026-07-17 | `message_box_display` carries `dependenton`/`contentwidth`; the client resolves the id to a `sap.ui.core.Element` before `MessageBox.show` |
| device-model-in-popups | 2026-07-17 | the shared `device>` model is bound on every view slot at creation — popup, popover and nested views, not just the main view |
| formatter-registry | 2026-07-18 | curated formatter module in the standard app layout (`app/webapp/model/formatter.js`, a served script, CSP-clean) — an eval-based `register_formatter` was implemented and reverted the same day for security |
| formatter-demokit-pack | 2026-07-18 | six suffixed demo kit formatters shipped in the curated module (`weightStateByValue`, `stockStatusState`/`Icon`, `round2DP`, `dimensions`, `deliveryStatusState`); beta sample `z2ui5_cl_demo_app_453` |
| binding-call | 2026-07-18 | declarative filter/sort on aggregation bindings via `cs_event-binding_call` (whitelisted methods/operators, built from data); beta samples `z2ui5_cl_demo_app_454`/`_455`. 2026-07-19: the interim `binding_call_by_id` wrapper method was consolidated into `follow_up_action` + `cs_event-binding_call` |
| message-model | 2026-07-18 | every view slot carries the central UI5 message model as `message>` with `handleValidation` registration; beta sample `z2ui5_cl_demo_app_458` |
| control-method-args | 2026-07-19 | `to` takes an optional transitionName, `open` an optional page key, `goToStep: [controlId, bool]` whitelisted; `castArgs` no longer pads missing trailing args (open() stays no-arg, no NaN ints). Found by hold-out probe #1 (apps 609/624/625) |
| control-methods-openby-setactivepage | 2026-07-20 | new `domRef` arg kind (control id → DOM element, control fallback) + `openBy: [domRef]` (unblocks app 016's hidden DatePicker; covers the TimePicker/Menu anchored-open family) and `setActivePage: [controlId]`. Found by batch b05 (apps 016/012). Note: app 012's Carousel re-sync stays dropped — template-clone page ids are not backend-addressable; an index-based page resolution would be a new request if more samples need it |
| binding-call-compound-filters | 2026-07-20 | `BINDING_CALL` `filter` accepts a JSON groups payload — OR inside each group, AND across groups, operators whitelisted, empty clears; the positional single-filter form is unchanged (a path can never start with `[`). Port 401 converted to the compound form — the ABAP-side model rebuild and its `t_products_all` mirror are gone |
| tree-expand-collapse | 2026-07-20 | `expandToLevel: ["int"]` and `collapseAll: []` added to the `CONTROL_BY_ID` whitelist (`app/webapp/core/FrontendAction.js` `CONTROL_METHODS`) for `sap.m.Tree` / `sap.ui.table.TreeTable`; the overview app's tree gains client-side Expand-all / Collapse-all buttons via `_event_client( cs_event-control_by_id … )` |
| menu-toggle-openby | 2026-07-20 | `toggleBy: ["domRef"]` added to `CONTROL_METHODS` — like `openBy`, but `control.isOpen() ? close() : openBy(anchor)`, so a press-to-toggle popup (`sap.m.Menu`) round-trips 1:1 without the backend mirroring open state. Unit tests added; b07 app 060 converted (openBy → toggleBy), IMPROVISED toggle-reduction dropped |
| message-manager-write (z2ui5.cc.MessageManager) | 2026-07-20 | new companion control `app/webapp/cc/MessageManager.js` bridging the UI5 message manager to a two-way bound ABAP table (`items`): app-authored rows are reconciled into the manager as `sap.ui.core.message.Message` (target + view model as processor → field valueState), the control's own rows removed when gone, auto-collected validation left untouched. Mirrors the MultiInputExt pattern; unit-tested; in the preload. Unblocks b08 app 065 (`MessagePopoverMessageHandling`) |
| formatter-inline-icon | 2026-07-20 | `expandInlineIcons(text)` added to the curated `model/formatter.js`: replaces `%%icon:sap-icon://<name>%%` placeholders with the `sapMMsgStripInlineIcon` markup (glyph via `IconPool`), the `MessageStripUtilities.getInlineIcon` equivalent — CSP-clean, no ABAP API. Unit tests added; b07 app 062's inlineIconsHelper converted (placeholders + `core:require` formatter binding), guessed-codepoint IMPROVISED dropped |
| message-toast-format | 2026-07-22 | merged upstream as abap2UI5 **#2466**: a `control_global` single-string method (MessageToast.show, MessageBox.*) composes its text from a template + client-resolved args (`{0}`,`{1}`,… filled by `$event.*`/`${$parameters>/…}`, plus the `{N?true:false}` toggle form), so a dynamic toast is roundtrip-free. `get_t_arg` quotes a leading `{0}` placeholder; lone strings unchanged. From apps 005, 060, 061, 077 |
| popover-bind-element | 2026-07-22 | merged upstream as abap2UI5 **#2466**: a `BIND_ELEMENT` `follow_up_action` (popup + popover) — `follow_up_action( val = cs_event-bind_element, view = popup/popover, t_arg = ( idx ) ( client->_bind( mt_tab ) ) )`. The binding comes from `_bind` (registered + rename-safe), never text; the action strips `{ }`, appends `/idx` and sets the slot's element binding. From app 094 |
| table-hidden-in-popin | 2026-07-22 | merged upstream as abap2UI5 **#2466**: `setHiddenInPopin: ["object"]` whitelisted in `CONTROL_METHODS`, so the auto-pop-in demo's MultiComboBox works. `setContextualWidth` / Slider `setWidth` stay unaddressed (no follow-up filed — no sample needs them). From app 092 |
| style-class-toggle | 2026-07-22 | merged upstream as abap2UI5 **#2466**: `addStyleClass`/`removeStyleClass`/`toggleStyleClass` (`["string"]`) whitelisted in `CONTROL_METHODS` — the client-side equivalent of the controller's `oControl.toggleStyleClass('x')`. Motivated by app 013 (its CSS is not shipped, so 013 stays as-is, but the capability is available) |
| split-container-nav | 2026-07-24 | merged upstream as abap2UI5 **#2470**: `toDetail`/`toMaster` (`["controlId"]`), `backDetail`/`backMaster` (no-arg), `setMode` (`["string"]`) and `navigateBack` (no-arg) whitelisted in `CONTROL_METHODS` — SplitApp/SplitContainer master-detail navigation + QuickView(Card) page back. Generic `evControlCallById` dispatch, no special-casing; 4 node tests. From apps 096/097/099/100 |
| scope-since-from-source | 2026-07-26 | implemented in **this repo's tooling** (no framework change): `generate-properties.mjs` emits each control's class-level `@since`/`@deprecated` into `ui5/properties.json`, and `scopeOf` (`generate-coverage.mjs`, plus the overview and status generators) falls back to it wherever `universe.json` carries null — the out-of-scope gate is authoritative offline. Surfaced the standing out-of-scope debt now tracked in STATUS.md. From apps 230–235 |
| unified-menu-open-anchored | 2026-07-27 | merged upstream as abap2UI5 **#2478**: the `openBy` dispatch falls back to `open(false, anchor, "begin top", "begin bottom", anchor)` when the resolved control has no own `openBy` (`sap.ui.unified.Menu`), so the same `openBy` wire works for every menu family. Apps 227/228 kept their wires — the declared no-op became functional; keyboard flag + explicit dock constants stay dropped (NOTE) |
| feedinput-enable-post-button | 2026-07-27 | merged upstream as abap2UI5 **#2478**: `enablePostButton: ["bool"]` listed in `CONTROL_METHODS` — a public method with no bindable-property equivalent (`enabled` disables the whole input, not just the Post button); the entry adds the X/space→boolean cast. App 236 rewired 1:1 |
| nav-app-call-caller-draft | 2026-07-27 | merged upstream as abap2UI5 **#2480**: Browser Back after a `nav_app_call` restores the CALLER as it was *rendered*, no longer dropping the client-side changes made before navigating. The response carries `nav_app_call_prev_app`/`_id` — the draft the caller was just saved under — and `View1._repointCallerEntry` repoints the caller's history entry at it before pushing the callee's route. From the overview app |
| formatter-date-empty-guard | 2026-07-30 | already merged upstream (abap2UI5 **#2482** touched `model/formatter.js` last; the guard `if (!s) return null;` ships with the exact Invalid-Date rationale as a source comment) — `DateCreateObject` returns `null` for a falsy input, so an optional date stays a missing value instead of a truthy Invalid Date. The port-side expression-binding guard and the `unguarded-date-formatter` pattern-lint rule stay as defense in depth: a system running an older framework release still needs the binding guard |
| core-commandexecution-keyboard-shortcuts | 2026-07-30 | merged upstream as abap2UI5 **#2482**: `cs_event-keyboard_shortcut` — `follow_up_action( t_arg = combo, event )` registers a document-level combo → named-backend-event binding (UI5 spelling + aliases, browser default suppressed, re-register rebinds, empty event unregisters, registry dies with the app switch). App 232 rewired: Ctrl+S/Ctrl+D registered on init, every `cmd:` button fires the same backend SAVE/DELETE/PSAVE events, commands gated server-side on their enabled/visible flags. Residual: no popover-local command scope (document-global registry) |
| event-prevent-default | 2026-07-30 | merged upstream (abap2UI5 `s_ctrl-check_prevent_default`): the event wire binds to `eBP`, which calls `oEvent.preventDefault()` synchronously before the round-trip — the event + args still reach the backend. App 241 rewired 1:1: every NavigationListItem press carries `check_prevent_default = prevent_default` (baked at render time; the checkbox got a declared `select` wire that redraws so the flag tracks the fresh state) |
| messagepopover-async-url | 2026-07-30 | merged upstream as abap2UI5 **#2482**: `setAsyncURLHandler: ["string"]` in `CONTROL_METHODS` naming a built-in URL policy (`ALLOW_ALL` / `RELATIVE_ONLY` / `DENY_ALL`) — the promise callback stays client-side, the wire carries data. App 067 rewired: `RELATIVE_ONLY` installed on init (the demo's http-gate case) + the original's `urlValidated` toast wired 1:1 |
| smartvariant-initialise | 2026-07-28 | merged upstream as abap2UI5 **#2481**: `cs_event-smart_variant_init` — the `initialise(fnCallback, oPersoControl)` handshake `sap.ui.comp` variant management needs and that no controller-less app can make. Without it `_oPersoControl` stays `null`, saving a view dies in `sap.ui.fl` and stored views never load. App 251 is live-verified (7 variants / 7 items survive a restart) and now names the action through the framework constant |
