{
  "class": "z2ui5_cl_ai_app_106",
  "sample": "sap.m.sample.SemanticPageFloatingFooter",
  "entity": "sap.m.semantic.SemanticPage",
  "file": "src/01/b13/z2ui5_cl_ai_app_106.clas.abap",
  "batch": "b13",
  "audit": {
    "frontend_action": false,
    "event_t_arg": true
  },
  "status": "generated",
  "checked": null,
  "deviations": [
    {
      "type": "NOTE",
      "what": "onSemanticButtonPress toasts each action's class name, reproduced by passing the name as a t_arg literal. The SortSelect change toasts the two-way selectedKey (sort_key); PagingButton positionChange transports ${$parameters>/newPosition}; the custom footer / share buttons transport $event.oSource.sId."
    },
    {
      "type": "IMPROVISED",
      "what": "The MultiSelectAction and MessagesIndicator handlers are simplified to fixed toasts, losing behavior the framework can express: the original toasts Pressed/Unpressed from the toggle state (readable via a ${$source>/pressed} t_arg) and opens a MessagePopover over the message model with one seeded 'Something wrong happened' error message (expressible via mvc:dependents + toggleBy per app 066, app-authored messages per app 065); without the seeded message the MessagesIndicator button stays invisible. Review 2026-07-27: needs rework, retyped from NOTE - a lost behavior is IMPROVISED, not NOTE."
    },
    {
      "type": "NOTE",
      "what": "The SortSelect items are bound to a 2-row filter-type table reproducing /ProductCollectionStats/Filters (the two `type` values Category and SupplierName; the per-type `values` sub-arrays are unused by the Select). The binding keeps the original sorter { path: 'Name' } 1:1 — a no-op there too, since the Filters entries carry `type`, not `Name`."
    },
    {
      "type": "NOTE",
      "what": "Same as SemanticPage but the MasterPage and DetailPage carry floatingFooter='true' and the MasterPage drops the PageAccessibleLandmarkInfo (matching the SemanticPageFloatingFooter variant)."
    }
  ]
}
