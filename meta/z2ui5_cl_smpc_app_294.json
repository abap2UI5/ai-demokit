{
  "class": "z2ui5_cl_smpc_app_294",
  "sample": "sap.m.sample.MessageViewWithGrouping",
  "entity": "sap.m.MessageView",
  "file": "src/02/01/z2ui5_cl_smpc_app_294.clas.abap",
  "batch": "b23",
  "audit": {
    "frontend_action": true,
    "event_t_arg": true,
    "note": "control_by_id navigateBack on the popup's MessageView"
  },
  "status": "reviewed",
  "checked": null,
  "deviations": [
    {
      "type": "NOTE",
      "what": "the three formatters are computed in the backend, which is the point of the thin-frontend rule (apps 009/010/022/092). buttonIconFormatter, buttonTypeFormatter and highestSeverityMessages each walk the whole message list to find the highest severity (Error > Warning > Success > Information) and count how many messages carry it - a computation over the data, not a presentation format. model_init does it once and the Button binds the three finished values (sap-icon://message-error, Negative, 5 for this data). The original re-runs the formatters on every model change; here the values are recomputed wherever the message list changes."
    },
    {
      "type": "NOTE",
      "what": "the original's view.xml is only the Page with the footer Button; the MessageView with its MessageItem template and Link, the Dialog with its custom-header Bar, the nav-back Button, the 'Publish order' Text and the Close endButton are all built in onInit. They are rebuilt here as a core:FragmentDefinition shown with popup_display, so Dialog, Bar, Text, MessageView, MessageItem, Link and two of the three Button controls are extra against the original view.xml."
    },
    {
      "type": "NOTE",
      "what": "the back button's visible state is two-way bound (visible={/BACK_VISIBLE}) instead of driven by setVisible, per the prefer-a-bindable-property rule; only navigateBack, which has no bindable equivalent, stays a control_by_id action on the popup slot. handleMessageViewPress's own navigateBack is not needed: popup_display rebuilds the fragment, so the MessageView always opens on its list page."
    },
    {
      "type": "NOTE",
      "what": "the MessageItem template binds counter={COUNTER} 1:1, but no message in the sample sets a counter - a flat ABAP row serializes it as 0, which ListItemBaseRenderer renders exactly like an absent counter (it only renders a truthy one). The two ungrouped messages carry no groupName, which is what makes them appear outside the two Purchase Order groups."
    },
    {
      "type": "POST_171",
      "what": "The four sap.m.ButtonType values the highest-severity button carries - Negative, Critical, Success, Neutral - are all @since 1.73 (sap.m/library.js; the Default/Accept/Reject/Transparent/Emphasized members above them carry none). The port computes them in ABAP and binds them onto Button.type, exactly the values buttonTypeFormatter returns upstream, so no gate can see them: the value never appears in the view, it arrives through the model. Declared by policy, the same route the sibling MessagePopover ports 066/067 take - both of which live under src/02 for it. Undeclared until 2026-08-23; declaring it moves the port from src/01/01 to src/02/01."
    }
  ]
}
