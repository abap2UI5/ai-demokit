{
  "class": "z2ui5_cl_smpc_app_133",
  "sample": "sap.f.sample.GridListModes",
  "entity": "sap.f.GridList",
  "file": "src/01/04/z2ui5_cl_smpc_app_133.clas.abap",
  "batch": "b02",
  "audit": {
    "frontend_action": true,
    "event_t_arg": true
  },
  "status": "reviewed",
  "checked": null,
  "deviations": [
    {
      "type": "NOTE",
      "what": "SegmentedButton.selectionChange reproduces onModeChange server-side: GridList.mode is two-way bound to the same field as SegmentedButton.selectedKey and the headerText (bound) is recomputed as 'GridList with mode ' + key on a backend round-trip. The GridList delete / selectionChange and the GridListItem press / detailPress are wired to client-side MessageToasts (the original toasted the affected item's runtime id). mode/headerText/selectedKey carry bindings the original set statically. **e2e-verified 2026-08-21** (the 2026-07-30 stamp was WRONG: the interaction's mode leg was shadowed by a duplicate key in the old in-file INTERACTIONS map and never ran once - found by the review sweep, and the two legs are merged into one module now) (transpiled-framework interaction, scripts/e2e-smoke.mjs): selecting 'SingleSelectLeft' round-trips the two-way bound mode: the GridList's mode follows and the header text the BACKEND composes ('GridList with mode SingleSelectLeft') appears, which nothing but a completed round-trip can produce. Until 2026-08-23 the leg clicked 'MultiSelect' - the mode the port already loads in - and SegmentedButton returns early when the pressed segment is the selected one, so no selectionChange fired and both assertions were already true at page load: the second stamp was as vacuous as the first, for a different reason."
    },
    {
      "type": "NOTE",
      "what": "The 11 product rows are inlined from the sample's model/data.json. Fields the original JSON omits are given their UI5 defaults so the bound enum-ish properties stay valid: GridListItem.type -> 'Inactive' (8 rows), highlight/Status -> 'None' (5 rows). Both render identically to the original's undefined values; structural-diff compares only binding paths, not data."
    },
    {
      "type": "NOTE",
      "what": "the four MessageToast handlers are reproduced as roundtrip-free CLIENT-COMPOSED toasts (follow_up_action cs_event-control_global MESSAGE_TOAST.show with a {N} template): onSelectionChange as '{0?Selected:Unselected} item with ID {1}' over ${$parameters>/selected} and ${$parameters>/listItem}.getId(), onDelete as 'Delete item with ID {0}', and onDetailPress/onPress as 'Request details for item with ID {0}' / 'Pressed item with ID {0}' over $event.oSource.sId. **Corrected 2026-08-01** (faked-event-value audit): all four used to toast a CONSTANT ('Selection changed', 'Delete item', 'Request details', 'Pressed item') and dropped the item identity the original shows. The ids are the runtime UI5 ids, exactly as in the original."
    },
    {
      "type": "NOTE",
      "what": "The sample's own main.css is archived and injected since 2026-08-21. The view carries class=\"imageDisplayBlock\" and the rule behind it (.imageDisplayBlock{display:block}) was missing entirely - the class name rendered with nothing behind it, which is exactly the gap apps 122/124 had and closed: 'a sample whose stylesheet is not in ui5/ is an archive gap to close, not a reason to drop the CSS'. main.css now sits in ui5/sap.f/GridListModes/ (closing the AGENTS section 4 archive gap) and reaches the view through a core:HTML style leaf."
    }
  ]
}
