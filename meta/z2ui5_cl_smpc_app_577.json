{
  "class": "z2ui5_cl_smpc_app_577",
  "sample": "sap.f.sample.FlexibleColumnLayoutColumnResize",
  "entity": "sap.f.FlexibleColumnLayout",
  "file": "src/02/04/z2ui5_cl_smpc_app_577.clas.abap",
  "batch": "b48",
  "audit": {
    "frontend_action": true,
    "event_t_arg": true,
    "note": "the anchor-bar section index and the columnResize beginColumn flag travel with the events; the section re-selection rides a control_by_id follow-up action addressing the section as an aggregation item"
  },
  "status": "generated",
  "checked": null,
  "deviations": [
    {
      "type": "POST_171",
      "what": "two post-1.71 members are kept for the 1:1 port: sap.f.FlexibleColumnLayout.autoFocus (since UI5 1.76) and restoreFocusOnBackNavigation (since 1.77), both set by the sample's own FCL view."
    },
    {
      "type": "NOTE",
      "what": "the sample is a Component with a ROUTER and three views (FlexibleColumnLayout, List, Detail). abap2UI5 serves one view, so the three become one: the FCL declares its begin and mid column pages inline and the routing collapses to the FCL's own layout property - toDetail opens the mid column (MidColumnFullScreen - see the maxColumnsCount note below) and handleClose closes it (OneColumn), which is what navTo(layout) does. Since 2026-08-31 the URL the router writes IS reproduced, via the framework's app-owned hash routing (cs_event-hash_attach_changed + hash_set, needs abap2UI5 newer than 1.144.0 - abap2UI5#2693, the app-012/584 recipe): toDetail pushes '#/detail/{layout}', handleClose returns the URL to the bare start - the empty ':section:' pattern, pushed as '/' which the Router normalizes to the empty app hash - the ':section:' list route ('#/3') carries the anchor-bar selection (see the section deviation), a hash change the app did not write (browser Back/Forward, a manual edit) round-trips as HASH_CHANGED, and a deep link / reload re-derives the state in view_display( ) (hash_apply is idempotent). The sample's own actionButtonsInfo helper state still has no counterpart - the close button is the only navigation action and it is always the right one to show while the mid column is open. Same fold as app 575."
    },
    {
      "type": "NOTE",
      "what": "the three archived views declare different default namespaces - FlexibleColumnLayout.view.xml has xmlns=\"sap.f\", List.view.xml xmlns=\"sap.uxap\" with m: for sap.m, and Detail.view.xml xmlns=\"sap.m\" with f: for sap.f. One abap2UI5 view can only have one default namespace (sap.m here), so every control of the list page carries a different PREFIX than in its original file even though it is the same control: ObjectPageLayout, ObjectPageDynamicHeaderTitle, ObjectPageSection and ObjectPageSubSection become uxap:-prefixed; Title, Button, Label, Text and form:SimpleForm lose their m: prefix; and the FlexibleColumnLayout gains an f: one. The whole missing/extra pairing of this port's structural diff is that shift - no control is actually added or dropped, and the doubled Title and Button counts are the two pages' own, counted together in one view."
    },
    {
      "type": "NOTE",
      "what": "the sample exists to show that a column RESIZE keeps the object page's scroll position: List.controller attaches columnResize on the FCL and calls setSelectedSection( sections[iSectionIndex] ) when the begin column goes back to one column, with the index kept in the URL by _updateUrlOnNavigate. Reproduced 1:1 since 2026-08-31 - both halves this deviation used to declare unreachable now exist. The URL half: the ':section:' route is the app-owned hash (see the routing deviation), written by the ObjectPage's navigate event, whose event argument computes indexOfSection where the control lives (${$parameters>/section}.getParent().indexOfSection(${$parameters>/section}) - the 575 thin-frontend idiom). The instance half: setSelectedSection takes a section INSTANCE whose clone ids only the client knows, so the follow-up addresses it positionally as an aggregation item - control_by_id ObjectPageLayout / setSelectedSection / ObjectPageLayout/sections/<index> (the pr/aggregation-item-address mechanism). Selected on the first rendering of a deep link (_onListMatched's bFirstRendering), re-selected when columnResize reports the begin column back at one column while the layout reads OneColumn (the original's guard, the DOM-free 575 spelling), and re-issued per rebuild because a rebuilt ObjectPage is back on its first section while section_ix survives as class state. Like the original, a plain browser Back over ':section:' URLs moves only the URL - only the first rendering and the column resize re-scroll, and handleClose drops the section from the URL exactly like navTo('list') without arguments (iSectionIndex NaN, here -1)."
    },
    {
      "type": "NOTE",
      "what": "webapp/data/sections.json is seeded verbatim - all twelve sections with their sectionName; tableName, which no control of this view binds, is carried along as the mock has it."
    },
    {
      "type": "NOTE",
      "what": "the close Button in the detail page's navigationActions is icon-only in the sample. The port gives it tooltip=\"Close column\" so it is reachable with a screen reader - the one accessibility addition in this port."
    },
    {
      "type": "NOTE",
      "what": "not yet verified in a running system: the mid-column open and close and the bound sections aggregation. **e2e-verified 2026-08-25** (nightly e2e interaction, meta/interactions/z2ui5_cl_smpc_app_577.mjs)."
    },
    {
      "type": "NOTE",
      "what": "mvc:View displayBlock=\"true\" has no counterpart: it is a Component-app setting that makes the view fill the page when it is the root of a Component, and abap2UI5 owns the view container itself."
    },
    {
      "type": "NOTE",
      "what": "toDetail sets MidColumnFullScreen, not TwoColumnsMidExpanded. This sample builds its semantic helper with maxColumnsCount: 1, and getNextUIState short-circuits to MidColumnFullScreen in that case - which is what hides the begin column and makes the return to OneColumn a begin-column resize from zero width, the event the sample exists to demonstrate."
    }
  ]
}
