{
  "class": "z2ui5_cl_ai_app_049",
  "sample": "sap.m.sample.StepInput",
  "entity": "sap.m.StepInput",
  "file": "src/01/b02/z2ui5_cl_ai_app_049.clas.abap",
  "batch": "b02",
  "audit": {
    "frontend_action": true,
    "event_t_arg": true,
    "note": "each StepInput change toasts `Value changed to '{0}'` roundtrip-free via control_global MESSAGE_TOAST.show ({0} filled by ${$parameters>/value}); the single quotes rely on get_t_arg's ' -> \\' escaping (abap2UI5 branch); the app is now init-only"
  },
  "status": "reviewed",
  "checked": null,
  "deviations": [
    {
      "type": "IMPROVISED",
      "what": "the sample binds a List to the JSON model /modelData and renders one templated CustomListItem per row. **Rebuilt as that ONE bound template on 2026-08-05** (it used to be 14 unrolled static items): the rows are bound with client->_bind( val = modeldata omit_initial = abap_true ), the parameter added upstream for exactly this shape (pr/model-empty-vs-default), so a property a row does not set stays ABSENT from the model and the StepInput keeps its own default instead of receiving ``. The dropped template property valueState is back as well. Residual: the two BOOLEAN properties cannot ride along - an explicit false is itself initial in ABAP, so omit_initial would drop it and the disabled/read-only rows would render enabled. They carry the original's literal in a string field and an expression binding converts it (enabled='{= ${ENABLED} !== \\'false\\' }', editable likewise), which is the only place the port's binding value differs from the original's {enabled}/{editable}. A path-scoped omit_initial would close that too - the open half of pr/model-empty-vs-default."
    },
    {
      "type": "NOTE",
      "what": "the change toast was switched to a roundtrip-free client-composed toast on 2026-07-22 (control_global MESSAGE_TOAST.show, template `Value changed to '{0}'` with get_t_arg single-quote escaping; on_event dropped, init-only) - re-verify changing a StepInput toasts \"Value changed to '<value>'\" with the quotes intact. **e2e-verified 2026-07-30** (transpiled-framework interaction, scripts/e2e-smoke.mjs): ArrowUp + Enter on the first StepInput fires change and toasts \"Value changed to '<value>'\" with the quotes intact; the other StepInputs are the identical wire."
    }
  ],
  "render_smoke": {
    "skip": true,
    "reason": "The rebuilt bound template binds the numeric StepInput properties min/max/step/value, and - like the ORIGINAL sample's aData - most rows do not set them (that is the point of the omit_initial rebuild). UI5 logs 'The value of property <p> must be a number' for such a row, so the render harness sees a console error that the running original produces just as well: the sample's own template binds min='{min}' over rows without a min. Nothing in the port is wrong, and seeding the numbers back would restore exactly the workaround this rebuild removed. Re-check when the render gate learns to treat an absent numeric path as the control default."
  }
}
