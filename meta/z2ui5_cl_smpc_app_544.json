{
  "class": "z2ui5_cl_smpc_app_544",
  "sample": "sap.m.sample.PlanningCalendarWeekNumbering",
  "entity": "sap.m.PlanningCalendar",
  "file": "src/02/01/z2ui5_cl_smpc_app_544.clas.abap",
  "batch": "b44",
  "audit": {
    "frontend_action": false,
    "event_t_arg": true
  },
  "status": "generated",
  "checked": null,
  "deviations": [
    {
      "type": "POST_171",
      "what": "sap.m.PlanningCalendar calendarWeekNumbering is @since 1.110.0 - newer than the 1.71 floor. The four week-numbering schemes ARE the sample, so the property is kept and the port is filed under src/02."
    },
    {
      "type": "NOTE",
      "what": "The calendar date properties (the PlanningCalendar startDate and every CalendarAppointment startDate and endDate) are typed \"object\" and demand a real JS Date. The model keeps ISO strings and Formatter.DateCreateObject from the curated module converts them at the point of use, the same idiom app 108 uses (needs UI5 >= 1.74)."
    },
    {
      "type": "NOTE",
      "what": "onCalendarWeekNUmberingSelect calls setCalendarWeekNumbering. The property is bindable and the sample's own view already binds the Select to the SAME model path, so the two share one field here and the change handler is dropped."
    },
    {
      "type": "NOTE",
      "what": "handleAppointmentSelect opens a MessageBox with the appointment title, its new selected state and the number of selected appointments - or, when the interval selection hit no appointment, their count. Every value is client-readable, so it travels with the event and ABAP composes both branches."
    },
    {
      "type": "NOTE",
      "what": "handleSelectionFinish hands the MultiComboBox's selected keys to setBuiltInViews. The picked keys travel as one comma-separated argument and ABAP fills the bound string table."
    },
    {
      "type": "NOTE",
      "what": "The sample's appointment rows carry an ariaHasPopup value the view never binds; the port seeds it the same way and leaves it unbound, so the data stays 1:1 with the mock."
    },
    {
      "type": "NOTE",
      "what": "The row images are the demo kit's own test-resources files, re-hosted on sdk.openui5.org."
    },
    {
      "type": "LIVE_TEST",
      "what": "The four week-numbering schemes, the appointment select and the built-in views box are unverified in a running system."
    }
  ]
}
