{
  "class": "z2ui5_cl_dmo_app_376",
  "sample": "sap.m.sample.StandardListItemWrapping",
  "entity": "sap.m.StandardListItem",
  "file": "src/01/z2ui5_cl_dmo_app_376.clas.abap",
  "batch": "b25",
  "audit": {
    "frontend_action": false,
    "event_t_arg": false
  },
  "status": "generated",
  "checked": null,
  "deviations": [
    {
      "type": "POST_171",
      "what": "infoIcon (since UI5 1.150) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.150 to render the status icon next to the info text."
    },
    {
      "type": "POST_171",
      "what": "infoStateInverted (since UI5 1.74) is newer than 1.71 but kept for the 1:1 port - it is what the sample's Toggle Inverted button drives."
    },
    {
      "type": "POST_171",
      "what": "wrapCharLimit (since UI5 1.94) is newer than 1.71 but kept for the 1:1 port - it is the property the last two rows of the sample demonstrate."
    },
    {
      "type": "NOTE",
      "what": "the original controller seeds only wrapping: false; /inverted is never seeded, so the Toggle Inverted button starts from an undefined model value that UI5 reads as not pressed. The port seeds inverted = abap_false explicitly, which is the same initial rendering - a flat ABAP row cannot carry an absent field."
    }
  ]
}
