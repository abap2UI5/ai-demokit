{
  "class": "z2ui5_cl_dmo_app_304",
  "sample": "sap.ui.unified.sample.CalendarMultipleMonth",
  "entity": "sap.ui.unified.Calendar",
  "file": "src/02/z2ui5_cl_dmo_app_304.clas.abap",
  "batch": "b15",
  "audit": {
    "frontend_action": false,
    "event_t_arg": true
  },
  "status": "generated",
  "checked": null,
  "deviations": [
    {
      "type": "NOTE",
      "what": "handleCalendarSelect/_updateText read the picked day off the control (oCalendar.getSelectedDates()[0].getStartDate()) and format it yyyy-MM-dd. The port transports it as three UI5 expression args (year, month+1, day - the LOCAL parts, not toISOString( ), which would shift the day east of Greenwich), each guarded by getSelectedDates().length > 0, and formats server-side; the Text gets a two-way bound text attribute instead of setText (app 139 idiom, probe-verified)."
    },
    {
      "type": "NOTE",
      "what": "handleSelectToday does removeAllSelectedDates() + addSelectedDate(new DateRange({startDate: today})) and reformats. The port only writes the text: the server date IS today, so the label is 1:1, but the calendar's own highlight of that day is not moved - addSelectedDate takes a DateRange CONTROL, which no wire can construct (same residual as app 139)."
    },
    {
      "type": "LIVE_TEST",
      "what": "not yet run in a system: the CAL_SELECT expression-arg round-trip across the two displayed months and the SELECT_TODAY text update."
    }
  ]
}
