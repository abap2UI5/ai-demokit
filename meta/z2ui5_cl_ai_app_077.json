{
  "class": "z2ui5_cl_ai_app_077",
  "sample": "sap.m.sample.NotificationListGroup",
  "entity": "sap.m.NotificationListGroup",
  "file": "src/01/b09/z2ui5_cl_ai_app_077.clas.abap",
  "batch": "b09",
  "audit": {
    "frontend_action": true,
    "event_t_arg": true,
    "note": "press/close/accept/reject all toast roundtrip-free via control_global MESSAGE_TOAST.show - press/close compose a template (`Item Pressed: {0}`/`Item Closed: {0}` + ${$source>/title}), so the app is init-only (the client-side removeItem on close is still not mirrored, see below)"
  },
  "status": "reviewed",
  "checked": null,
  "deviations": [
    {
      "type": "NOTE",
      "what": "onItemClose is reproduced 1:1 since 2026-08-05. The original removes the item from its list (oList.removeItem(oItem)) and toasts its title; the port does BOTH client-side on the same event: UI5 runs a ';'-separated pair of event handlers (measured with scripts/probes/event-arg-expression-probe.mjs), so the wire chains control_by_id removeItem with the client-composed toast. The item travels as its own id ($event.oSource.getId()), which ManagedObject.removeAggregation accepts (measured in the same probe). The NotificationList gained an `id` the original does not carry - removeItem needs a target for the wire; that is the port's only extra attribute here. The earlier 'static items, so close only toasts' rationale is retired. Group 3's onAcceptErrors keeps its accept toast."
    },
    {
      "type": "NOTE",
      "what": "the original's showCloseButton=\"falseue\" typo on two items is corrected to false, otherwise UI5 boolean parsing rejects it."
    },
    {
      "type": "POST_171",
      "what": "the NotificationList container control (since UI5 1.90) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.90 to render it (control-level, invisible to the member-level property gate)."
    }
  ]
}
